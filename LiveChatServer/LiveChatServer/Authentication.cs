using System;
using System.Collections.Generic;
using System.Text;

namespace LiveChatServer
{
    public class Authentication
    {
        public static bool RegistrationSucceed(string name, string hashPass)
        {
            return true;
        }

        public static bool LoginSucceed(string name, string hashPass)
        {
            return true;
        }
    }
}
